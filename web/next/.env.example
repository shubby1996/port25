# Leave blank when the backend is reverse-proxied on the same origin.
# For local Next development use: NEXT_PUBLIC_PORT25_API=http://localhost:8000
NEXT_PUBLIC_PORT25_API=http://localhost:8000

# Required only for the CopilotKit generative-UI chat/runtime.
OPENAI_API_KEY=
